# ============================================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# ============================================================
# Este archivo es un FALLBACK - se usa si no existe .env.development o .env.production
#
# MODO DESARROLLO (npm run dev): usa .env.development → localhost:8080
# MODO PRODUCCIÓN (npm run build): usa .env.production → Railway
#
# Vercel automáticamente usa modo producción al hacer deploy
# ============================================================

VITE_API_URL=http://localhost:8080/api
